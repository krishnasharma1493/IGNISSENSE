# SIH 26162 ML Pipeline Dependencies
# Python >= 3.12 (rasterio 1.5 wheels)

# Core
numpy>=1.24.0
pandas>=2.0.0
scikit-learn>=1.3.0
xgboost==3.4.1

# Data
requests>=2.31.0

# Real training pipeline (labelling + dataset build)
duckdb>=1.5.0          # FIRMS archive joins, OSM PBF filtering, mining-polygon spatial join
pyarrow>=15.0.0        # parquet label/candidate files
rasterio>=1.5.0        # ESA WorldCover footprint fractions (wheel bundles GDAL)
openpyxl>=3.1.0        # EOG VIIRS Nightfire flare catalogues (.xlsx)

# Serialization
joblib>=1.3.0

# Explanation (not used by the inference path)
shap>=0.43.0

# Notebooks
jupyter>=1.0.0
matplotlib>=3.7.0
seaborn>=0.12.0

# API (for inference service, if needed)
# flask>=3.0.0
