# Environment variables & Secrets
.env
.env.*
*.env
*.env.*
!.env.example
!.env.*.example

# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class

# C extensions
*.so

# Distribution / packaging
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# Virtual environments
venv/
.venv/
env/
ENV/
env.bak/
venv.bak/

# Jupyter Notebook checkpoints
.ipynb_checkpoints/

# Heavy ML model dumps and cache (retain schemas and metadata)
models/*.joblib
models/*.pkl
models/xgb_fire_classifier*.joblib
models/xgb_fire_classifier*.json
!models/model_metadata.json
!models/feature_schema.json
!models/LIMITATIONS.json

# The active artifact IS committed (5.2 MB + 8.2 MB): without it service.py has
# nothing to load and the backend silently degrades to other_or_uncertain @ 0.50.
# Superseded v1 and synthetic artifacts stay ignored.
!models/xgb_fire_classifier_v2.joblib
!models/xgb_fire_classifier_v2.json

# Raw/processed data (retain gitkeeps and demo directories)
data/raw/*
data/processed/*
# Training intermediates: local mongod data, extracted features, candidates (GBs)
data/interim/*
!data/raw/.gitkeep
!data/processed/.gitkeep
!data/demo/

# OS & Editor
.DS_Store
.vscode/
.idea/
*.swp
*.swo
