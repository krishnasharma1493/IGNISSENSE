# OS files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Environment and secrets
.env
.env.*
!.env.example
*.pem
*.key

# Dependencies and build artifacts
node_modules/
dist/
build/
.cache/
*.pyc
__pycache__/

# IDE and Editor files
.idea/
.vscode/
*.swp
*.swo

# Antigravity artifacts (transcripts, tasks)
.system_generated/
scratch/

# Rendered video, demo footage and the notes that go with it.
# Kept locally; the film is attached to the GitHub Release instead.
*.mp4
*.mov
demo-video/

# Agent and local tooling. These stay on disk so the tools keep working,
# they just are not part of the published project.
brag-output/
brag-output-*/
.superpowers/
.impeccable/
skills-lock.json
AppleSKILL.md
CLAUDE.md

# Working notes and early specification drafts, superseded by README.md
ARCHITECTURE-PROMPT.txt
PRDmain.md

# Design mockups and agent session write-ups, not application code
stitchDash/
docs/

# Empty placeholder; the real tests live in backend/src/**/*.test.ts
tests/
